<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
    <title><%= title %></title>
</head>
<body class="bg-gray-100">
    <%- include('header.ejs') %>
    <div class="container mx-auto mt-8 p-4">
        <div class="grid grid-cols-7">
            <div class="col-start-3 col-span-3 bg-white p-8 rounded-3xl shadow-2xl">
                <h1 class="text-2xl font-semibold mb-6">Change Passwoed</h1>
                <form action="/changepassword" method="post">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="mb-4">
                        <label for="newpassword" class="block text-gray-700">New Password:</label>
                        <input type="password" name="newpassword" id="newpassword" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
                    </div>
                    <div class="mb-4">
                        <label for="retyped" class="block text-gray-700">Retype new Password:</label>
                        <input type="password" name="retyped" id="retyped" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
                    </div>
                    <div class="flex flex-row mb-4">
                        <button type="submit" class="bg-green-600 text-white font-medium rounded px-5 py-2 hover:bg-green-800">Update Password</button>
                        <div class="mx-2">
                            <% if (messages && messages.error && messages.error.length > 0) { %>
                                <% for (var i = 0; i < messages.error.length; i++) { %>
                                    <p class="text-red-700 font-mono font-semibold text-base mt-2">! <%= messages.error[i] %></p>
                                <% } %>
                            <% } %>
                        </div>
                    </div>
                </form>
                <p>Don't have an account yet? <a href="/signup/educator" class="text-green-600">Sign up</a></p>
            </div>
        </div>
    </div>
</body>
</html>
