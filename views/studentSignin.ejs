<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
    <title><%= title %></title>
</head>
<body class="bg-gray-100">
    <%- include('header.ejs') %>
    <div class="container mx-auto mt-8 p-4 md:w-2/3 lg:w-1/3">
        <div class="bg-white p-4 rounded-3xl shadow-2xl">
            <h1 class="text-2xl font-semibold mb-4 text-center">Login to your account</h1>
            <form action="/student/login" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="mb-4">
                    <label for="email" class="block text-gray-700">Email:</label>
                    <input type="email" name="email" id="email" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="mb-4">
                    <label for="password" class="block text-gray-700">Password:</label>
                    <input type="password" name="password" id="password" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="mb-4">
                    <button type="submit" class="bg-green-600 text-white font-medium rounded px-5 py-2 hover:bg-green-800 w-full">Sign in</button>
                </div>
            </form>
            <div class="text-center">
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for (var i = 0; i < messages.error.length; i++) { %>
                        <p class="text-red-700 font-mono font-semibold text-base mt-2">! <%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <p class="mt-4 text-left">Don't have an account yet? <a href="/signup/student" class="text-green-600">Sign up</a></p>
        </div>
    </div>
</body>
</html>
